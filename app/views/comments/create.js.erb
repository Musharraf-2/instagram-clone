$(".comment-errors").empty()
$("#comment_body").val("")
<% if @comment.errors.any? %>
  <% @comment.errors.full_messages.each do |msg| %>
    $(".comment-errors").append("<%= msg %><br>");
  <% end %>
<% else %>
  $(".comments-div").prepend("<%= j(render(@comment)) %>");
<% end %>
