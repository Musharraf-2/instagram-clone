<%= form_with model: [@post, @comment], local: false do |form| %>
  <%= form.label "Comment" %>
  <%= form.text_field :body, class: "form-control", id: "comment_body" %>
  <br />
  <%= form.submit nil, class: "btn btn-primary mb-4" %>
  <%= render "shared/errors", object: @comment %>
<% end %>

