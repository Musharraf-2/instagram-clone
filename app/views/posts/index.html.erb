<div class="container">
  <% if user_signed_in? %>
    <%= link_to "Create Post", new_post_path, class: "btn btn-primary" %>
  <% end %>
  <div class="row justify-content-center">
    <div class="col-6">
      <%= render @posts %>
    </div>
  </div>
</div>
